/* =========================
   M4BREW — THEME SYSTEM
   Clean, intuitive color labels for easy theming
========================= */

:root {
  /* =========================
     VERSION
  ========================= */
  --ui-version: "1.7.0";

  /* =========================
     LAYOUT & SPACING
     (These stay the same across themes)
  ========================= */
  --page-pad: 22px;
  --page-gap: 22px;
  --inset-pad: 24px;
  --list-indent: 18px;
  --radius: 18px;
  --radius-pill: 999px;
  --font-size: 10pt;
  --line-height: 1.35;
  --pill-h: 44px;
  --pill-pad-x: 16px;
  --stack-pad: 22px;

  /* Page-specific (legacy support) */
  --task-pad: var(--page-pad);
  --task-inset: var(--inset-pad);
  --history-pad: var(--page-pad);
  --history-inset: var(--inset-pad);
  --settings-gap: 18px;

  /* =========================
     CORE COLOR PALETTE
     Change these hex values to create new themes
  ========================= */
  
  /* Task colors (3 steps) */
  --taskcolour1: #0264BF;  /* Step 1: Convert - Blue */
  --taskcolour2: #B958A7;  /* Step 2: Rename - Pink */
  --taskcolour3: #5250A1;  /* Step 3: Delete - Purple */
  
  /* Background */
  --background: #111318;   /* Main page background */
  
  /* Buttons */
  --buttons1: #222043;     /* Primary/selected state */
  --buttons1-rgb: 34 32 67;   /* buttons1 in RGB for transparency */
  --buttons2: #1F2128;     /* Secondary/unselected state */
  
  /* Panel backgrounds */
  --panel-bg-2: var(--buttons2);   /* Status pill default */
  
  /* Text */
  --font1: #FFFFFF;        /* Main text (white) */
  --font2: #A6A7AE;        /* Muted text (grey) */
  --font3: #6B6C72;     /* Very dim text - placeholders, disabled */
  --shadow: rgba(0,0,0,.08);  /* Box shadows */
  
  /* UI Elements */
  --outlines: #323539;     /* All borders/dividers/lines */
  
  /* Status */
  --warning: #C26400;      /* Orange warning */
  --failed: #FF4000;       /* Red error */
  
  /* Special */
  --empty: transparent;    /* Transparent fills */

  /* =========================
     DERIVED COLORS
     (Auto-generated from core palette)
  ========================= */
  
  /* RGB values for alpha blending */
  --taskcolour1-rgb: 2 100 191;
  --taskcolour2-rgb: 185 88 167;
  --taskcolour3-rgb: 82 80 161;
  
  /* Surfaces (subtle overlays on background) */
  --surface: rgba(255, 255, 255, 0.04);
  --surface-strong: rgba(255, 255, 255, 0.06);
  --surface-soft: rgba(255, 255, 255, 0.03);
  --surface-lift: rgba(255, 255, 255, 0.05);
  
  /* Shadows */
  
  /* Overlays/modals */
  --overlay-dim: rgba(0, 0, 0, 0.55);
  --modal-bg: rgba(20, 22, 28, 0.96);

  /* =========================
     LEGACY ALIASES
     (For backwards compatibility with existing CSS)
  ========================= */
  --bg: var(--background);
  --border: var(--outlines);
  --line: var(--outlines);
  --stack-line: var(--outlines);
  --accent: var(--taskcolour3);
  --accent-soft: rgba(82, 80, 161, 0.28);
  --accent-border: rgba(82, 80, 161, 0.55);
  --danger: var(--failed);
  --warn: var(--warning);
  --warn-outline: rgba(255, 157, 0, 0.55);
  --warn-soft: rgba(255, 157, 0, 0.14);
  --task-convert: var(--taskcolour1);
  --task-rename: var(--taskcolour2);
  --task-delete: var(--taskcolour3);
  --task-convert-rgb: var(--taskcolour1-rgb);
  --task-rename-rgb: var(--taskcolour2-rgb);
  --task-delete-rgb: var(--taskcolour3-rgb);
  --outline-brand: rgba(82, 80, 161, 0.38);
  --test-outline: var(--outline-brand);
  --outline-brand-strong: rgba(82, 80, 161, 0.55);
  --font: var(--font-size);
  --lh: var(--line-height);
}

/* =========================
   LIGHT THEME
   (Optional - can be removed if not needed)
========================= */

/* =========================
   LATTE THEME
   Warm, creamy, coffee-inspired palette
========================= */
[data-theme="latte"] {
  /* Task colors */
  --taskcolour1: #E39C4C;  /* Convert - Warm orange */
  --taskcolour2: #BB9C7D;  /* Rename - Tan */
  --taskcolour3: #CF8C68;  /* Delete - Terracotta */
  
  /* Background */
  --background: #FEF9F3;   /* Cream */
  
  /* Buttons */
  --buttons1: #D6C1A4;
  --buttons1-rgb: 214 193 164;     /* Light cream */
  --buttons2: #F4EBD6;     /* Darker cream */
  
  /* Text */
  --font1: #6F5B57;        /* Dark brown */
  --font2: #B5A097;        /* Medium grey */
  --font3: #A6A7AE;        /* Light grey */
  
  /* UI Elements */
  --outlines: #BB9C7D;     /* Tan borders */
  
  /* Status */
  --warning: #C26400;      /* Orange warning */
  --failed: #FF4000;       /* Red error */
  
  /* Special */
  --empty: transparent;
  --shadow: rgba(0,0,0,.08);
  
  /* Panel backgrounds */
  --panel-bg-2: var(--buttons2);
  
  /* RGB values */
  --taskcolour1-rgb: 227 156 76;
  --taskcolour2-rgb: 187 156 125;
  --taskcolour3-rgb: 207 140 104;
  
  /* Aliases (for compatibility) */
  --bg: var(--background);
  --border: var(--outlines);
  --line: var(--outlines);
  --stack-line: var(--outlines);
}

/* =========================
   ESPRESSO THEME
   Rich, dark, coffee-inspired palette
========================= */
[data-theme="espresso"] {
  /* Task colors */
  --taskcolour1: #E39C4C;  /* Convert - Warm orange */
  --taskcolour2: #BB9C7D;  /* Rename - Tan */
  --taskcolour3: #CF8C68;  /* Delete - Terracotta */
  
  /* Background */
  --background: #1F1505;   /* Dark brown */
  
  /* Buttons */
  --buttons1: #352306;
  --buttons1-rgb: 53 35 6;     /* Darker brown - selected */
  --buttons2: #4F3102;     /* Medium brown - unselected */
  
  /* Text */
  --font1: #F4EBD6;        /* Cream text */
  --font2: #AAA089;        /* Tan muted */
  --font3: #706958;        /* Brown dim */
  
  /* UI Elements */
  --outlines: #7D552D;     /* Brown borders */
  
  /* Status */
  --warning: #C26400;      /* Orange warning */
  --failed: #FF4000;       /* Red error */
  
  /* Special */
  --empty: transparent;
  --shadow: rgba(0,0,0,.08);
  
  /* Panel backgrounds */
  --panel-bg-2: var(--buttons2);
  
  /* RGB values */
  --taskcolour1-rgb: 227 156 76;
  --taskcolour2-rgb: 187 156 125;
  --taskcolour3-rgb: 207 140 104;
  
  /* Aliases */
  --bg: var(--background);
  --border: var(--outlines);
  --line: var(--outlines);
  --stack-line: var(--outlines);
}
