services:
  m4brew:
    image: matxby/m4brew:1.4.8
    container_name: m4brew
    ports:
      - "8585:8080"
    environment:
      - TZ=Europe/London
      - PUID=1026
      - PGID=100
      - DOCKER_NETWORK=matt-net
    volumes:
      - /mnt/remotes/192.168.4.4_media/Audiobooks:/audiobooks_remote:rw,slave
      - /mnt/cache/media/Audiobooks:/AudioBook Local:rw,slave
      - /mnt/cache/appdata/m4brew/config:/config:rw
      - /var/run/docker.sock:/var/run/docker.sock:rw
    networks:
      - matt-net
    restart: unless-stopped

networks:
  matt-net:
    external: true
